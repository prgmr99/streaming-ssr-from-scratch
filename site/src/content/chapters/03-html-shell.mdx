---
title: HTML 껍데기 붙이기
summary: 문자열로 조립할 것인가, React 트리 안에 넣을 것인가
order: 3
---

{/* 3단계. 응답이 div부터 시작한다는 문제. */}
{/* 방식 A는 header와 trailer 문자열을 앞뒤로 붙인다. */}
{/* 방식 B는 html부터 React 트리 안에 넣는다. */}
{/* React가 DOCTYPE을 자동 주입한다는 발견. */}
{/* 스트림에 개입한다는 것, 즉 read 다음 enqueue의 의미. */}

## React가 만드는 것은 div부터다
